# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 로컬 스크래치(워크스페이스 규칙: 미추적)
_local/

# 컷오버 스크립트 생성 산출물 — 프로덕션 주문/결제 데이터 포함, 커밋 금지(로컬 provenance 전용)
# (도구 자체 .mjs/.sql·테스트 골든 hash-goldens.json 은 추적 유지)
scripts/refund/paid-credit-allocation.json
scripts/refund/pre-0062-drain.json
scripts/refund/preflight-portone-legacy.json
